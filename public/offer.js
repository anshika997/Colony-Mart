function openOffer(offerName) {
    alert(`You are viewing the ${offerName}! Enjoy the savings!`);
    // You can redirect to a specific offer page if needed:
    // window.location.href = `offers/${offerName}.html`;
}
